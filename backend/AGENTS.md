# AGENTS.md

Default to using `bun` instead of `node`.

Prefer `ccc search "<query>"` instead of `find` or `grep`.

Always prompt the user for confirmation before proceeding with task to avoid unwanted actions.